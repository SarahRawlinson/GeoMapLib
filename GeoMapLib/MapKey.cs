namespace GeoMapLib;

public class MapKey
{
    public string Name;
    public string Key;
    public string Colour;

    public MapKey(string name, string key, string colour)
    {
        Name = name;
        Key = key;
        Colour = colour;
    }
}